# ============================================================
# EnergiCerdas AI — Backend (FastAPI) Requirements
# ============================================================

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0

# Data & numerik
pandas>=2.2.0
numpy>=2.0.0

# Machine learning
# Versi di-pin PERSIS sesuai lingkungan pelatihan model (dsm_metadata.json,
# lingkungan_pelatihan) -- joblib.dump() ikut nge-pickle kelas dasar
# scikit-learn LGBMClassifier, jadi versi yang beda saat load berisiko
# error (pernah terjadi: _ARRAY_API not found).
scikit-learn==1.6.1
lightgbm==4.6.0
joblib==1.5.3

# Gen AI
google-generativeai>=0.7.0